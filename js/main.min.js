"use strict";var scene=new THREE.Scene,geometry=new THREE.BoxGeometry(100,100,100),material=new THREE.MeshLambertMaterial({color:255}),mesh=new THREE.Mesh(geometry,material);scene.add(mesh);var point=new THREE.PointLight(16777215);point.position.set(400,200,300),scene.add(point);var ambient=new THREE.AmbientLight(4473924);scene.add(ambient);var width=window.innerWidth,height=window.innerHeight,k=width/height,s=200,camera=new THREE.OrthographicCamera(-s*k,s*k,s,-s,1,1e3);camera.position.set(200,300,200),camera.lookAt(scene.position);var renderer=new THREE.WebGLRenderer;renderer.setSize(width,height),renderer.setClearColor(12178431,1),document.body.appendChild(renderer.domElement),renderer.render(scene,camera);